import 'package:flutter/material.dart';

class CcColors {
  CcColors._();

  // BRAND

  static const Color primary = Color(0xFF0AB5A8);
  static const Color primaryDark = Color(0xFF078A80);
  static const Color primaryDarker = Color(0xFF055F58);

  static const Color secondary = Color(0xFFFF7B3A);

  static const Color accent = Color(0xFF94EDE7);

  // BACKGROUNDS

  static const Color background = Color(0xFFF7FBFC);

  static const Color surface = Color(0xFFFFFFFF);

  static const Color surfaceSecondary = Color(0xFFF0FDFC);

  static const Color surfaceTertiary = Color(0xFFE6FAF9);

  static const Color overlay = Color.fromRGBO(0, 0, 0, 0.35);

  // TEXT

  static const Color textPrimary = Color(0xFF1A2B35);

  static const Color textSecondary = Color(0xFF3D5566);

  static const Color textTertiary = Color(0xFF7A96A4);

  static const Color textInverse = Color(0xFFFFFFFF);

  static const Color textDisabled = Color(0xFF7A96A4);

  // BORDERS

  static const Color border = Color(0xFFE4EEF2);

  static const Color borderFocused = primary;

  static const Color borderError = Color(0xFFFECDD3);

  // STATUS

  static const Color success = Color(0xFF22C55E);

  static const Color error = Color(0xFFBE123C);

  static const Color errorDark = Color(0xFF9F1239);

  static const Color errorBackground = Color(0xFFFFF1F2);

  // SPECIAL

  static const Color navy = Color(0xFF0D1F2D);

  static const Color navyLight = Color(0xFF0A2A3A);

  static const Color tealIcon = Color(0xFF0F766E);

  // GRAYS

  static const Color gray100 = Color(0xFFE8EEF4);

  static const Color gray200 = Color(0xFFDDE6ED);

  // WHITE OPACITY

  static const Color white94 = Color.fromRGBO(255, 255, 255, 0.94);

  static const Color white80 = Color.fromRGBO(255, 255, 255, 0.80);

  static const Color white70 = Color.fromRGBO(255, 255, 255, 0.70);

  static const Color white65 = Color.fromRGBO(255, 255, 255, 0.65);

  static const Color white60 = Color.fromRGBO(255, 255, 255, 0.60);

  static const Color white50 = Color.fromRGBO(255, 255, 255, 0.50);

  static const Color white25 = Color.fromRGBO(255, 255, 255, 0.25);

  static const Color white20 = Color.fromRGBO(255, 255, 255, 0.20);

  static const Color white15 = Color.fromRGBO(255, 255, 255, 0.15);

  static const Color white12 = Color.fromRGBO(255, 255, 255, 0.12);

  // PRIMARY OPACITY

  static const Color primary12 = Color.fromRGBO(10, 181, 168, 0.12);

  static const Color primary15 = Color.fromRGBO(10, 181, 168, 0.15);

  static const Color primary18 = Color.fromRGBO(10, 181, 168, 0.18);

  static const Color primary25 = Color.fromRGBO(10, 181, 168, 0.25);

  // SHADOWS

  static const Color shadowStrong = Color.fromRGBO(13, 31, 45, 0.15);

  static const Color shadowMedium = Color.fromRGBO(13, 31, 45, 0.08);

  static const Color shadowLight = Color.fromRGBO(13, 31, 45, 0.05);

  static const Color shadowSubtle = Color.fromRGBO(13, 31, 45, 0.04);

  // UTILITY

  static const Color transparent = Colors.transparent;
}
