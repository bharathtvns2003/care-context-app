name: splash_screen
description: "Splash screen feature for Care Context."
version: 0.0.1
publish_to: "none"

environment:
  sdk: ^3.10.7
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  design:
    path: ../../design
  core:
    path: ../../core
  firebase_auth: ^5.5.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
